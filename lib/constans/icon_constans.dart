import 'package:flutter/material.dart';

class IconsConstans {
  static const exitIcon = Icon(Icons.cancel_presentation_outlined, size: 30); // Icons.check_box,
  static const okIcon = Icon(Icons.check_box, size: 30); // Icons.check_box,
  static const addIcon = Icon(Icons.add_box, size: 30); // Icons.check_box,
  static const fullokIcon = Icon(Icons.playlist_add_check, size: 30); // Icons.check_box,
  static const sinifIcon = Icon(Icons.home_work, size: 30); // Icons.check_box,
  static const dersIcon = Icon(Icons.play_lesson, size: 30); // Icons.check_box,
  static const temrinIcon = Icon(Icons.work, size: 30); // Icons.check_box,
  static const ogrenciIcon = Icon(Icons.wc, size: 30); // Icons.check_box,
  static const warningIcon = Icon(Icons.warning, size: 30); // Icons.check_box,
  static const settingsIcon = Icon(Icons.settings, size: 30); // Icons.check_box,
  static const puanlarIcon = Icon(Icons.calculate, size: 30); // Icons.check_box,
  static const temrinnotIcon = Icon(Icons.add_chart_rounded, size: 30); // Icons.check_box,
  static const saveIcon = Icon(Icons.save, size: 30); // Icons.check_box,
  static const gelmediIcon = Icon(Icons.night_shelter, size: 30); // Icons.check_box,
}
